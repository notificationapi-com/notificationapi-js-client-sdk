{
  "name": "vanillajs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./build && mkdir build && cp index.html ./build/index.html && cp -r ../../dist/ build",
    "deploy": "../../node_modules/.bin/gh-pages -d build --dest vanillajs --user \"Automated Action <noreply@notificationapi.com>\"",
    "predeploy": "npm run build"
  },
  "author": "",
  "license": "ISC"
}
